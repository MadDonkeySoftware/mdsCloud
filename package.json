{
  "name": "root",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build",
    "dev:identity:server": "npm -w identity run dev:server",
    "format": "lerna run format",
    "lint": "lerna run lint",
    "publish": "lerna publish",
    "refresh:cli": "npm -w mds-cloud-cli run prepublish:setup",
    "test": "lerna run test",
    "test:cov": "lerna run test:cov"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@types/node": "^20.4.2",
    "lerna": "^7.1.4",
    "typescript": "^5.1.6"
  }
}
